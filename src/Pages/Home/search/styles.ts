import styled from '@emotion/styled';

import { PageWrapper } from 'Layouts';

export const SearchWrapper = styled(PageWrapper)`
  max-width: initial;
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 12px;
`;
